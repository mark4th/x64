\ utf8 table dump.
\ ------------------------------------------------------------------------

  .( loading utf8.f ) [cr]

\ ------------------------------------------------------------------------

: .utf8  ( to from --- )
  hex
  begin
    2dup <>
  while
     dup 8 mod 0=
     if
       cr cr
       dup 0 <# # # # # # # # # #> type space
     then
     dup (utf8) space
     1+
  repeat
  decimal ;

\ ========================================================================
